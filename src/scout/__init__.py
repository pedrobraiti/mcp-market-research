"""Scout — a stateless MCP server that gathers and structures financial data for AI agents.

The senses layer of an agentic-trading ecosystem: it reads the market and returns typed,
consolidated data. It never places orders and never persists user state — same question,
same world. Decisions and memory live in the calling agent / strategy skill.
"""

__version__ = "0.4.1"
